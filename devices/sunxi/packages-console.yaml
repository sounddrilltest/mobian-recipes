{{- $architecture := or .architecture "arm64" -}}
{{- $device := or .device "pinephone" }}

architecture: {{ $architecture }}

actions:
  - action: run
    description: Tweaks for console
    chroot: true
    script: ../../scripts/setup-console.sh

